export interface ActionModalType {
    userId: number | undefined,
    termId: string
}

export interface TermIdType {
    termId: number | string
}

export interface UserId {
    userId: number | undefined
}
